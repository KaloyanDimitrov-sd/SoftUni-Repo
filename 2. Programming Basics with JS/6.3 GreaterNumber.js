function greaterNumber(input1, input2) {
    let fnum = Number(input1);
    let snum = Number(input2);
    if (fnum > snum) {
        console.log(fnum);
    }else {
        console.log(snum);
    }
}

greaterNumber("10", "5")